extends layout

block content
  section.hero.is-link.is-fullheight-with-navbar.error-hero
    .hero-body
      .container
        p.title
          | #{error.status} - #{message}
        p.subtitle
          a(href="/")
            i.fas.fa-home
            |  Go to home page
  //-
    pre #{error.stack}
